/* Pass-through for now — this is where the /garden route's own fonts and
   palette (CSS variables) will live (board-3 aesthetic). */
export default function GardenLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
