/* Pass-through for now — this is where the /about route's own fonts and
   palette (CSS variables) will live. */
export default function AboutLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
