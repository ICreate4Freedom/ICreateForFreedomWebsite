/* Pass-through for now — this is where the /writing route's own fonts and
   palette (CSS variables) will live (board-1 aesthetic). */
export default function WritingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
