/* Pass-through for now — this is where the /projects route's own fonts and
   palette (CSS variables) will live (board-2 aesthetic). */
export default function ProjectsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
