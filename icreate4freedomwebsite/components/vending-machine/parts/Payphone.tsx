/* The phone booth was removed — it pulled focus from the machine. This slot
   in the scene is intentionally empty now; the right-side world-building lives
   in EnvironmentBack. Kept the export so VendingMachine's wiring is unchanged. */
export function Payphone() {
  return null;
}
